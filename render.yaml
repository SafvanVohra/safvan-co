services:
  - type: web
    name: vohra-fullstack
    env: node
    rootDir: .
    branch: main
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
